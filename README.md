# Boundary-Creation-UI
 A desktop application for loading DXF files, extracting point data, and generating **alpha shape boundaries** interactively.   Built with **PySide6 (Qt6)**, **matplotlib**, and **alphashape**.
